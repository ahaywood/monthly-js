---
const { number, heading } = Astro.props
---

<div class="relative max-w-xs">
  <div class="font-wide text-eerieBlack text-[200px] text-center leading-none z-0 absolute -top-4 left-1/2 -translate-x-1/2">{number}</div>
  <div class="relative z-10">
    <h4 class="mb-14 font-wide uppercase text-base leading-5 z-20 text-center pt-[75px] min-h-[135px] text-hotMagenta">{heading}</h4>
    <slot />
  </div>
</div>